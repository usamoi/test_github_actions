name: Tests

on:
  push:
    branches:
      - main

jobs:
  rustup_test:
    name: rustup_test
    runs-on: "ubuntu-22.04"

    strategy:
      matrix:
        alpha: [0, 1, 2, 3]
        beta: [0, 1, 2, 3]

    steps:
      - uses: actions/checkout@v4

      - name: Run
        run: |
          env
          cat ~/.rustup/settings.toml
          sha512sum ~/.cargo/bin/rustup

          cargo clippy
